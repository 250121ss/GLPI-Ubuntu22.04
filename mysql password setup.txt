sudo chmod -R 755 /home/username/
===  Install Required Dependencies:   === 
sudo apt update
sudo apt install mariadb-server
sudo mysql_secure_installation

sudo apt -y install php php-{curl,zip,bz2,gd,imagick,intl,apcu,memcache,imap,mysql,cas,ldap,tidy,pear,xmlrpc,pspell,mbstring,json,iconv,xml,gd,xsl}
****
sudo mysql -u root -p
CREATE DATABASE glpi;
CREATE USER 'glpiuser'@'localhost' IDENTIFIED BY 'StrongDBPassword';
GRANT ALL PRIVILEGES ON glpi.* TO 'glpiuser'@'localhost';

FLUSH PRIVILEGES;
EXIT;

-==== Mysql password change commands === 

USE database;
UPDATE glpi_users SET `password` = MD5('new_password') WHERE name IN ('glpi', 'post-only', 'tech', 'normal');

COMMIT;
exit;
