sudo chmod -R 755 /home/username/
===  Install Required Dependencies:   === 

sudo apt install apache2 mariadb-server php php-{cli,bz2,gd,intl,ldap,mbstring,mysql,xml,zip,curl,apcu,gmp,json}
****
sudo mysql

CREATE USER 'glpiuser'@'localhost' IDENTIFIED BY 'verystongpassword';
GRANT ALL PRIVILEGES ON glpidb.* TO 'glpiuser'@'localhost';
FLUSH PRIVILEGES;
EXIT;

-==== Mysql password change commands === 

USE database;
UPDATE glpi_users SET `password` = MD5('new_password') WHERE name IN ('glpi', 'post-only', 'tech', 'normal');

COMMIT;
exit;
